pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Simple git checkout
                git branch: 'main', url: 'https://github.com/GokulRajiMahendiran/go-ni.git'
            }
        }
        // Add additional stages as needed
    }
}
